import { UserIcon } from "@heroicons/react/solid";
import React from "react";

type detailRowProps = {
  edit: Boolean;
  label: string;
  data: string;
  employeeState: {};
  employeeDetailsHandler: Function;
};

const DetailRow = ({
  edit,
  label,
  data,
  employeeState,
  employeeDetailsHandler,
}: detailRowProps) => {
  return;
};
